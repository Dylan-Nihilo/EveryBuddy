---
name: setup
description: Set up EveryBuddy companion in Claude Code — hatches a companion and configures the status bar.
allowedTools:
  - Bash
---

Run this exact command:

node ${CLAUDE_PLUGIN_ROOT}/dist/index.js install claude-code

Show the full output. After success, tell the user to restart Claude Code.
